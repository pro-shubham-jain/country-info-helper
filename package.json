{
  "name": "@country-info-helper/common",
  "version": "1.0.6",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "rmdir /s /q build",
    "tsc": "tsc -p tsconfig.json",
    "build": "npm run  tsc",
    "start": "nodemon src/examples.ts",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish --access public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@types/lodash": "^4.14.197",
    "nodemon": "^3.0.1",
    "path": "^0.12.7"
  }
}
